spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/store?createDatabaseIfNotExist=true
    username: root
    password: "#Olamide21#"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
stripe:
  apiUrl: https://api.stripe.com
  apiKey: key_diekalceizcdjadjeapx
  enabled: true
  supported-currencies: USD,GBP,EUR

payment-gateway: "paypal"

email:
  host: "SMTP"
  port: 34829